apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Test Pipeline (Feature Branches)

# This workflow runs on PRs targeting master/alpha/beta
# Triggers on PR open, new commits pushed to PR, and PR reopen
# For direct pushes to master/alpha/beta, the combined Test and Publish Pipeline runs instead

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - master
      - alpha
      - beta

jobs:
  test:
    steps:
      - name: Checkout Code
        uses: cloudbees-io/checkout@v1

      - name: Run Tests
        uses: docker://node:18
        env:
          SF_LOGIN_URL: ${{ secrets.SF_LOGIN_URL }}
          SF_USERNAME: ${{ secrets.SF_USERNAME }}
          SF_PASSWORD: ${{ secrets.SF_PASSWORD }}
        run: |
          echo "=== Installing jq and SF CLI ==="
          apt-get update && apt-get install -y jq curl
          npm install -g @salesforce/cli
          sf --version
          
          echo "=== Authenticating to Salesforce ==="
          if [ -z "$SF_LOGIN_URL" ] || [ -z "$SF_USERNAME" ] || [ -z "$SF_PASSWORD" ]; then
            echo "ERROR: Missing Salesforce credentials."
            echo "Configure SF_LOGIN_URL (var), SF_USERNAME (var), SF_PASSWORD (secret) in CloudBees."
            exit 1
          fi
          
          LOGIN_RESULT=$(curl -s --max-time 30 "${SF_LOGIN_URL}/services/oauth2/token" \
            -d "grant_type=password" \
            -d "client_id=PlatformCLI" \
            -d "client_secret=" \
            -d "username=${SF_USERNAME}" \
            -d "password=${SF_PASSWORD}")
          
          ACCESS_TOKEN=$(echo "$LOGIN_RESULT" | jq -r '.access_token // empty')
          INSTANCE_URL=$(echo "$LOGIN_RESULT" | jq -r '.instance_url // empty')
          
          if [ -z "$ACCESS_TOKEN" ]; then
            echo "ERROR: OAuth2 login failed. Response:"
            echo "$LOGIN_RESULT" | jq . 2>/dev/null || echo "$LOGIN_RESULT"
            exit 1
          fi
          
          echo "$ACCESS_TOKEN" | sf org login access-token --instance-url "$INSTANCE_URL" --no-prompt --json 2>&1
          echo "Authenticated as: $SF_USERNAME"
          
          echo "=== Installing dependencies ==="
          npm install
          
          echo "=== Running Unit Tests ==="
          npm run-script unitTest
          
          echo "=== Linking package ==="
          npm link
          
          echo "=== Running Vlocity Test Job (Verbose) ==="
          vlocity -sfdx.username "$SF_USERNAME" runTestJob --verbose
          
          echo "=== Running Vlocity Test Job (JSON) ==="
          vlocity -sfdx.username "$SF_USERNAME" runTestJob --json | jq .
          
          echo "=========================================="
          echo "TEST PIPELINE - PASSED"
          echo "=========================================="
